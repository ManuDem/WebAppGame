# Scripts

Script di supporto non parte del runtime principale.

- `legacy/update_scene.js`: utility storica one-shot, mantenuta solo per riferimento.

Linee guida:
- non usare questi script nel flusso build/test standard
- preferire script idempotenti con input/path relativi al repository
